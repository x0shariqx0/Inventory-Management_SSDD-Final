apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.mongo.storage.pvcName }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.mongo.storage.size }}
